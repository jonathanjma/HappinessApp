class User {
  constructor(id, email, profile_picture, password, settings, username) {
    this.id = id;
    this.email = email;
    this.profile_picture = profile_picture;
    this.password = password;
    this.settings = settings;
    this.username = username;
  }
}